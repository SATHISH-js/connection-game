services:
  - type: web
    name: connection-game
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST_AUTH_PIN
        value: "1234"
      - key: MONGODB_URI
        sync: false
